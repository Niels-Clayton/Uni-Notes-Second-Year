/**
 * viewing_buffer_editor.cc
 *
 * Student ID: 300437590
 * Name: Niels Clayton
 */

#include "viewing_buffer_editor.h"

using namespace viewing;
using namespace std;

/**
 * Load the contense of @param load into the viewing buffer, starting from
 * the provided @param row @param col
 */
void ViewingBuffer::setBuffer(char *load, int length, int row, int col){ };

/**
 * Scroll through the viewing buffer by @param lines, in direction @param direction
 * with positive values of direction scrolling up, and negative scrolling down
 */
void ViewingBuffer::scroll(int lines, int direction){ };

/*
 * toggle wrapping of the viewing buffer, and return a bool of if wrapping is enabled
 */
bool ViewingBuffer::enableWrap(){ };

/*
 * toggle numbering along the left side of the viewing buffer
 * return the boolean value of if toggling is on
 */
bool ViewingBuffer::numbering(){};

int main()
{


}